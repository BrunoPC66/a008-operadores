const par = +prompt('Digite um número par:');

console.log('Resto do nº div por 2:', par%2);

// Com números pares sempre resta 0, pois par/par é exata.
// Com números impares sempre resta 1, pois impar/par não é exata.

